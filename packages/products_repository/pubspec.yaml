name: products_repository
description: A Dart repository which manages the product domain
version: 1.0.0+1
publish_to: none

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5
  json_annotation: ^4.7.0
  flutter_bloc: ^8.1.1
  dio: ^4.0.6
  products_api:
    path: ../products_api

dev_dependencies:
  json_serializable: ^6.5.4
  build_runner: ^2.3.3